# Настройки подключения к PostgreSQL для Jira
# Используйте эти параметры в ваших клиентах PostgreSQL

Хост: localhost
Порт: 5432
Пользователь: jira
Пароль: jira123
База данных: jira

# Строка подключения для JDBC
JDBC_URL: jdbc:postgresql://localhost:5432/jira
JDBC_USER: jira
JDBC_PASSWORD: jira123
JDBC_DRIVER: org.postgresql.Driver

# Команды для подключения:
# 1. Через psql:
#    PGPASSWORD=jira123 psql -h localhost -p 5432 -U jira -d jira

# 2. Через скрипт:
#    ./connect-postgres.sh

# 3. Через Docker:
#    docker exec -it jira-postgres psql -U jira -d jira
